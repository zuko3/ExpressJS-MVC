const fs = require('fs');
const path = require("path");
const rootPath = require('../util/path');

module.exports = class Cart {
    static addProducts(id, productPrice) {
       
    }

    static deleteProduct(productId, price, cbFunc) {
    }

    static getCart(cbFunc) {
    }
}